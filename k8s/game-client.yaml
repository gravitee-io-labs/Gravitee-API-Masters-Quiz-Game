apiVersion: apps/v1
kind: Deployment
metadata:
  name: quiz-game-client
  namespace: quiz-game
  labels:
    app.kubernetes.io/name: quiz-game-client
    app.kubernetes.io/part-of: gravitee-quiz
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: quiz-game-client
  template:
    metadata:
      labels:
        app.kubernetes.io/name: quiz-game-client
        app.kubernetes.io/part-of: gravitee-quiz
    spec:
      containers:
        - name: game-client
          image: dobl1/quiz-game-client:1.0.5
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              name: http
          env:
            - name: API_BASE_URL
              valueFrom:
                configMapKeyRef:
                  name: quiz-frontend-config
                  key: API_BASE_URL
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: quiz-game-client
  namespace: quiz-game
  labels:
    app.kubernetes.io/name: quiz-game-client
    app.kubernetes.io/part-of: gravitee-quiz
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      name: http
  selector:
    app.kubernetes.io/name: quiz-game-client
